var W__class_name__ = class W__class_name__ { // var is used because it creates reference on globalThis (window) object

  // use json.child for single child widget like Center
  // or json.children for array of widgets
  /**
   * @typedef __class_name__JSONType
   * @prop {string} text
   * 
   * @typedef {__class_name__JSONType & WidgetJSON} __class_name__JSON
   */

  /**
   * @param {__class_name__JSON} json
   * @returns {HTMLElement}
   */
  static build (json) {
    return html ({
      
    });
  }

  /**
   * @param {__class_name__JSON} json
   * @returns {HTMLElement}
   */
  static edit (json) {

  }

  /**
   * @param {HTMLElement} element
   * @returns {JSON}
   */
  static destruct (element) {

  }
};